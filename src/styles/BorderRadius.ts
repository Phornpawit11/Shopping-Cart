export const borderRadius = {
    perfectNestedRadius: (innerRadius: number, padding: number) => {
        const outerRadius = innerRadius + padding
        return { innerRadius, outerRadius }
    },

}
export const MAIN_BORDER_RADIUS_1 = 7
export const MAIN_BORDER_RADIUS_2 = 10
export const MAIN_BORDER_RADIUS_3 = 15
export const MAIN_BORDER_RADIUS_4 = 20
